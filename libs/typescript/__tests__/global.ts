import { describe, test } from 'vitest'
import { setup } from '@nuxt/test-utils'

describe('Teste de components globais', async () => {
  await setup({
    // contexto do teste
  })
  test('inicial', () => {
    // teste
  })
})